# Tests in Lambda

Note that this will actually delete files, so we need to be careful and also need to copy files so there is something to delete each time we test.


test

```json
{
  "queryStringParameters": {
    "history_code": "dc61707d-1c7c-4809-a677-8f6c03572762"
  }
}
```
